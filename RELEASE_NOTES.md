# DYWorker 更新内容

## 0.1.21 新增

- **浏览器导入站点数据**：新增 localStorage 导入，kimi 等新式站点的登录态可以随导入一起迁移；会话级 Cookie 自动补有效期，导入的登录态跨重启保留
- **渠道支持更换工作目录**：微信/QQ 对话里直接发「更换工作目录至 …」即可切换该聊天的操作目录
- **渠道媒体消息**：微信/QQ 渠道支持接收图片、文件、语音，并对媒体消息链路做了加固
- **外部路径授权优化**：本人批准的工作区外路径在本次任务内持续有效（含子路径），不再重复询问；替我审批模式改由审核助手按上下文判断
- **消息右键编辑菜单**：排队消息「调整方向」改为「立即执行」
- **任务控制与模型补强**：任务支持停止、输入提示与分片上传；工具参数按 JSON Schema 前置校验，输出被截断时不执行工具调用，补齐 V4 Pro 视觉识别，网络抖动自动重试
- **修复**：历史导入按 BigInt 读取大时间戳，技能（工作模板）改为渐进披露

## 0.1.20 新增

- **排队消息升级为队列卡片**：任务运行期间发送的消息统一收在输入框上方，可调整执行方向、编辑内容或移除，不再插进对话流
- **发送/停止按钮合并**：任务运行中，输入框的发送键自动变成停止键，操作更直观
- **任务完成提示音**：任务结束时播放轻提示音（本地合成，不联网）
- **上下文超限自动兜底**：服务端判定上下文超限时，自动压缩后重试，不再直接中断任务

## 新功能

- **侧边聊天**：右侧面板新增临时聊天窗口，随手提问不打扰主对话，关闭应用后不保留记录
- **文件预览**：文件面板支持 Markdown 和常见文本文件内联预览，带语法高亮、文件筛选和路径定位
- **Git 审阅**：新增审阅面板，可以按版本对比查看代码改动、切换/新建分支、提交和推送
- **浏览器数据导入**：支持把浏览器中的 Cookie、密码、浏览记录导入应用（兼容常见国产 Linux 浏览器）
- **会话排队**：任务运行期间继续发送消息会自动排队，排队消息在执行前可以编辑或取消

## 体验优化

- 模型出错时给出更易懂的提示（如内容安全拦截、服务异常、网络问题）
- 长对话自动整理：上下文接近上限时自动压缩早期内容，减少中途中断
- 长消息自动折叠，代码块一键复制，图片点击预览
- 右侧面板增加菜单和快捷键，切换更顺手

## 问题修复

- 修复自动更新失效：应用内之前一直检查不到新版本，本次修复后可在应用内直接检查并更新
- 修复 macOS 自动更新签名校验失败：无证书构建的包补齐资源封印签名，下载的更新可以正常安装
- 修复更新对话框错误文本超出窗口：长路径/长错误信息自动换行
- 修复会话输出未结束时提前显示消息时间的问题
- macOS 首次打开被系统拦截时，提供一键放行脚本（scripts/macos-open-fix.command）

## 安装包

- Windows x64：dyworker-0.1.21-windows-x64-setup.exe
- macOS（Apple 芯片）：DYWorker-0.1.21-arm64.dmg / .zip
- Linux ARM64（麒麟等）：dyworker-0.1.21-arm64.deb / .AppImage
