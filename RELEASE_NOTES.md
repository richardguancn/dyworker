# DYWorker 更新内容

## 0.1.20 新增

- **排队消息升级为队列卡片**：任务运行期间发送的消息统一收在输入框上方，可调整执行方向、编辑内容或移除，不再插进对话流
- **发送/停止按钮合并**：任务运行中，输入框的发送键自动变成停止键，操作更直观
- **任务完成提示音**：任务结束时播放轻提示音（本地合成，不联网）
- **上下文超限自动兜底**：服务端判定上下文超限时，自动压缩后重试，不再直接中断任务

## 新功能

- **侧边聊天**：右侧面板新增临时聊天窗口，随手提问不打扰主对话，关闭应用后不保留记录
- **文件预览**：文件面板支持 Markdown 和常见文本文件内联预览，带语法高亮、文件筛选和路径定位
- **Git 审阅**：新增审阅面板，可以按版本对比查看代码改动、切换/新建分支、提交和推送
- **浏览器数据导入**：支持把浏览器中的 Cookie、密码、浏览记录导入应用（兼容常见国产 Linux 浏览器）
- **会话排队**：任务运行期间继续发送消息会自动排队，排队消息在执行前可以编辑或取消

## 体验优化

- 模型出错时给出更易懂的提示（如内容安全拦截、服务异常、网络问题）
- 长对话自动整理：上下文接近上限时自动压缩早期内容，减少中途中断
- 长消息自动折叠，代码块一键复制，图片点击预览
- 右侧面板增加菜单和快捷键，切换更顺手

## 问题修复

- 修复自动更新失效：应用内之前一直检查不到新版本，本次修复后可在应用内直接检查并更新
- macOS 首次打开被系统拦截时，提供一键放行脚本（scripts/macos-open-fix.command）

## 安装包

- Windows x64：dyworker-0.1.20-windows-x64-setup.exe
- macOS（Apple 芯片）：DYWorker-0.1.20-arm64.dmg / .zip
- Linux ARM64（麒麟等）：dyworker-0.1.20-arm64.deb / .AppImage
