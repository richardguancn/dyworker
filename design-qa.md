# 工作目录操作界面对照验收

## 对照目标

- source visual truth: `/var/folders/6h/rpqn03ds5sb4_jmp35lcbl8h0000gr/T/codex-clipboard-af2fca63-b22e-43c8-9fb1-434891212123.png`
- secondary source: `/var/folders/6h/rpqn03ds5sb4_jmp35lcbl8h0000gr/T/codex-clipboard-985fe159-030f-40f3-a0a5-df8c56db1398.png`
- implementation screenshot: `/tmp/dyworker-workspace-menu.png`
- focused implementation crop: `/tmp/dyworker-workspace-menu-crop.png`
- viewport: 1280 × 720 CSS px
- source pixels: 784 × 190；implementation pixels: 1280 × 720；focused crop: 300 × 190
- density normalization: 浏览器与截图均按 1 倍密度检查；局部截图保持 190 px 同高并列对照
- state: 工作目录已展开，“更多”菜单打开，未置顶

## 全视图与局部对照

工作目录行已包含“更多”和“新建对话”两个图标入口；菜单从工作目录行右侧向下展开，包含图标和两项操作，没有被侧栏裁剪。参考图是局部放大图，项目现有界面采用更紧凑的侧栏密度，因此保留原有字号和行高，避免只为局部功能破坏整体一致性。

本次没有图片、品牌图形或装饰素材，沿用项目现有图标库。字体、颜色、圆角、阴影和间距均复用现有侧栏与任务菜单样式。菜单文案按系统显示：macOS 为 Finder，Windows 为文件资源管理器，Linux 为文件管理器。

## 交互验收

- 点击工作目录“更多”：菜单正常打开，点击外部或按 Esc 可关闭。
- 点击“置顶项目”：出现已置顶标记，工作目录排序优先，状态随会话记录保存；再次打开菜单可取消置顶。
- 点击“新建对话”：立即在对应工作目录下生成并打开空白对话，输入框获得焦点。
- 工作目录菜单与单条任务菜单互斥，不会同时叠加。
- 浏览器控制台错误：0。
- 本地浏览器预览无法调用桌面系统的文件管理器；桌面调用沿用项目已有且已接通的打开目录能力，并由专项检查覆盖。

## 五项检查

- 字体与文字：沿用应用现有字体、字号与字重；菜单文案完整，无异常换行。
- 间距与布局：操作按钮与目录标题同排，菜单右对齐且未裁剪。
- 颜色与视觉：沿用现有浅色、深色变量和悬停反馈。
- 图片与图标：无新增图片资产；全部使用项目现有图标库。
- 内容：新建对话、置顶/取消置顶及三平台文件管理器名称均已覆盖。

## 对照历史

### 第一次检查

- 发现：如果先打开单条任务菜单，再打开工作目录菜单，两者可能同时显示。
- 修复：两个菜单入口现在会先关闭对方，再打开自己。

### 最终检查

- 未发现可阻断交付的 P0/P1/P2 差异。
- P3：参考图的控件比例更大；为保持 DYWorker 现有侧栏密度，本次没有整体放大目录行。

final result: passed
